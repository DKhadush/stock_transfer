<mvc:View controllerName="com.mindsquare.stock.transfer.controller.werkauswahl" 
		xmlns:mvc="sap.ui.core.mvc" 
		displayBlock="true"
		xmlns="sap.m" 
		xmlns:core="sap.ui.core" 
		xmlns:html="http://www.w3.org/1999/xhtml"
		xmlns:smartTable="sap.ui.comp.smarttable" 
		xmlns:customData="http://schemas.sap.com/sapui5/extension/sap.ui.core.CustomData/1">
    <App id="GR_App">
        <Page title="{i18n>appTitle}" showNavButton="true">
            <content>
            	<HBox>
            		<!-- Werk Auswahl -->
            		<VBox id="werksComboVBox" layoutData="sap.m.FlexItemData" class="sapUiSmallMargin">
	                    <Label text="{i18n>plant}" />
	                    <ComboBox id="werksComboBox" placeholder="{i18n>selectPlant}" selectionChange=".onPlantSelectionChange" items="{Werks>/}">
	                        <core:Item key="{Werks>Werks}" text="{Werks>Werks} {Werks>Name1}" />
	                    </ComboBox>
                	</VBox>
	                
	                <!-- Nach Werk Auswahl -->
	                <VBox id="zwerksComboVBox" layoutData="sap.m.FlexItemData" class="sapUiSmallMargin">
	                    <Label text="{i18n>destplant}" />
	                    <ComboBox id="zwerksComboBox" placeholder="{i18n>selectPlant}" selectionChange=".onDestPlantSelectionChange" items="{Werks>/}">
	                        <core:Item key="{Werks>Werks}" text="{Werks>Werks} {Werks>Name1}" />
	                    </ComboBox>
	                </VBox>
            	</HBox>
                <HBox>
                	<!-- Lagerort Auswahl -->
	                <VBox id="lgortComboVBox" layoutData="sap.m.FlexItemData" class="sapUiSmallMargin">
	                    <Label text="{i18n>storageLocation}" />
	                    <ComboBox id="lgortComboBox" placeholder="{i18n>selectStorageLocation}" selectionChange=".onStorageLocationChange" items="{Lgort>/}">
	                        <core:Item key="{Lgort>Lgort}" text="{Lgort>Lgort} {Lgort>Lgobe}" />
	                    </ComboBox>
                	</VBox>
                
                	<!-- Ziellagerort Auswahl -->
	                <VBox id="zlgortComboVBox" layoutData="sap.m.FlexItemData" class="sapUiSmallMargin">
	                    <Label text="{i18n>deststorageLocation}" />
	                    <ComboBox id="zlgortComboBox" placeholder="{i18n>selectStorageLocation}" selectionChange=".onStorageLocationChange" items="{Lgort>/}">
	                        <core:Item key="{Lgort>Lgort}" text="{Lgort>Lgort} {Lgort>Lgobe}" />          
	                    </ComboBox>
	                </VBox>
                </HBox>
            </content>
            <footer>
					<Bar class="sapContrast sapContrastPlus">
						<contentRight>
							<Button id="btnStock" text="{i18n>loadingMaterials}" class="sapUiLargeMargins" icon="sap-icon://save" type="Accept" data:id="bookingDialog"
								press="navToBestand" visible="false"/>
						</contentRight>
					</Bar>
				</footer>
        </Page>
    </App>
</mvc:View>
